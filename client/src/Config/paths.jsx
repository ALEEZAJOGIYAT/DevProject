const HOME = `/`
const LOGIN = `/login`
const SIGNUP = `/signup`
const PROJECTPAGE = `/project`
const ACTIVE = `/activate`


export default {
    HOME,
   LOGIN,
   SIGNUP,
   PROJECTPAGE,
   ACTIVE
}